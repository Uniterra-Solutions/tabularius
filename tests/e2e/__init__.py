"""Docker E2E tests (docs/testing.md)."""

# __init__.py makes pytest collect these as the `e2e.*` package so the
# basenames don't collide with the top-level unit test files
# (test_cli.py / test_provider.py).
